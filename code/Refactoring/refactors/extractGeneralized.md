# Extract Generalized ([Extract subclass](https://refactoring.guru/extract-subclass), [superclass](https://refactoring.guru/extract-superclass), [interface](https://refactoring.guru/extract-interface))

Features are used only in certain cases: Extract subclass.
Features are exactly the same in mutliple classes: Extract superclass.
Features are vaguely the same in mutliple classes: Extract interface.

How to do it:
