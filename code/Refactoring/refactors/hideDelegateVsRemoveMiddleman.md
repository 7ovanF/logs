Hide Delegate: The less the client knows, the better. Hide "delegate" object (object that contains functionality needed by client).
Remove Middleman: When a middleman does nothing except pass on data.

It's... really difficult to remove the middleman. A class may **really** make sense holding a certain object.
An alternative is probably to pass on something more specific about the information, so that there IS logic.

gawd, do i really need to read the smell list?