# Bootstrap

tldr; Kickstarter

The thing sewed to the back of boots to slip in feet.

In Computer: the very first program/piece of code executed to initialize all aspects of the system. solves chicken and egg problem (OS needs code to initialize itself)

Stored in firmware/ROM (modern computers).
> firmware/ROM: [is firmware in ROM?](2-IsFirmwareInROM.md)

(some of) What it initializes: 
- CPU Registers
- device controllers
- memory contents
It must locate where the kernel 

>[Bootstrap program = BIOS?](3-BootstrapEqualsBIOS.md)